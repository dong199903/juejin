import Vue from 'vue'
import Vuex from 'vuex'
Vue.use(Vuex)

import editorModule from './editorModule'

const state = {
	
}

const mutations = {
	
}

const actions = {
	
}

const modules = {
	editorModule
}

export default new Vuex.Store({
	state,
	actions,
	mutations,
	modules,
})